import * as user from '../lib/users';

import db from './db';

import should from 'should';

describe( 'Site Get', () => {
    before( ( done ) => {
        db( done );
    });

    it( 'should fail on invalid token');
});
